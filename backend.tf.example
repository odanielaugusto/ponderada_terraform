terraform {
  backend "s3" {
    bucket         = "nome-do-bucket-de-estado"
    key            = "ponderada-terraform/dev/terraform.tfstate"
    region         = "us-west-2"
    dynamodb_table = "terraform-locks"
    encrypt        = true
  }
}
